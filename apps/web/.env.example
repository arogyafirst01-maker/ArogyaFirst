# Frontend Environment Variables Template for ArogyaFirst Web App
# Copy this file to .env.local and update values as needed for local development

# Backend API Base URL - Used by authFetch to connect to the Express API server
# Note: Vite requires the VITE_ prefix to expose environment variables to the client-side code
# Do not include the /api suffix in the URL, as API routes already include it
# Update this for production deployment (e.g., https://api.arogyafirst.com)
VITE_API_BASE_URL=http://localhost:3000

# Application Name - Used for branding and display purposes
VITE_APP_NAME=ArogyaFirst

# Application Version - Current version of the frontend app
VITE_APP_VERSION=0.1.0

# Maximum file size for uploads (in bytes, 5MB default for client-side validation)
VITE_MAX_FILE_SIZE=5242880

# Allowed file types for document uploads (used by FileInput accept prop)
VITE_ALLOWED_FILE_TYPES=.pdf,.jpg,.jpeg,.png

# Razorpay Payment Gateway Configuration

# Razorpay API Key ID for client-side checkout (public key, safe to expose)
# This should match the RAZORPAY_KEY_ID from the backend .env file
# Use test keys (rzp_test_) for development, live keys (rzp_live_) for production
# Note: Only the Key ID (not the secret) should be exposed to the frontend
VITE_RAZORPAY_KEY_ID=rzp_test_your_key_id

# Payment timeout in milliseconds (default: 900000 = 15 minutes)
# Maximum time allowed for completing a payment before session times out
# User will receive timeout error if payment is not completed within this duration
# Note: This is a client-side timeout only; server-side refunds handle abandoned payments
VITE_PAYMENT_TIMEOUT=900000

# Enable analytics tracking (set to true for production)
VITE_ENABLE_ANALYTICS=false

# Sentry DSN for error tracking (leave empty to disable)
VITE_SENTRY_DSN=

# Agora Video Call Configuration

# Agora App ID for client-side video call initialization (public identifier, safe to expose)
# This should match the AGORA_APP_ID from the backend .env file
# Get this from https://console.agora.io/ by creating a new project
# Note: App Certificate should NEVER be exposed to frontend - tokens are generated on backend
# Free tier provides 10,000 minutes per month for development and testing
VITE_AGORA_APP_ID=your_agora_app_id_here
